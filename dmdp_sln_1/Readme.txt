// monolith problems
// exiting solution -- open existing class with more code
  pain points - monolith , coupled, large methods and unstrctured, deployment issues, webservices 
               - monitoring issue (health checks) , metrics (no of hits(throughput), error by type), request response logs
               -- log analysers, APM (infra + application) tool integration
               -- Rate limitter

   
    // architeture -- design pattrens -- singleton, dynamic injection, Factory
    // Solid priciples ***   SIngle responsibility,openclose,LSP,Interface segregation,DIP
    // DRY , KISS ,YAGNI
    // exception handling centralised
    // logs
    
    // using block -- object lifecycle
    // openclose -- extending object -- class initialise and modify
    // unit test
    // add UI layer